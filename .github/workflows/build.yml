name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-dev python3-pip build-essential
          sudo apt-get install -y libssl-dev libffi-dev zlib1g-dev
          sudo apt-get install -y openjdk-17-jdk wget unzip zip
          sudo apt-get install -y autoconf libtool

      - name: Install Python packages
        run: |
          pip install --upgrade pip
          pip install buildozer cython

      - name: Set up Android SDK
        run: |
          # Создаем папку для SDK
          mkdir -p /opt/android-sdk/cmdline-tools
          cd /opt/android-sdk/cmdline-tools
          
          # Скачиваем command line tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          rm commandlinetools-linux-9477386_latest.zip
          
          # Перемещаем в правильную структуру (нужно в папку latest)
          mv cmdline-tools latest
          
          # Устанавливаем пути
          export ANDROID_SDK_ROOT=/opt/android-sdk
          export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools
          
          # Принимаем лицензии (игнорируем ошибки)
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true
          
          # Устанавливаем нужные компоненты
          $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-31" "build-tools;30.0.3" > /dev/null 2>&1 || true
          $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager "build-tools;31.0.0" > /dev/null 2>&1 || true
          
          echo "ANDROID_SDK_ROOT=/opt/android-sdk" >> $GITHUB_ENV
          echo "PATH=$PATH" >> $GITHUB_ENV
          
          # Проверяем установку
          ls -la $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/

      - name: Create buildozer.spec
        run: |
          echo '[app]' > buildozer.spec
          echo 'title = TG WS Proxy' >> buildozer.spec
          echo 'package.name = tgwsproxy' >> buildozer.spec
          echo 'package.domain = com.tgwsproxy' >> buildozer.spec
          echo 'source.dir = .' >> buildozer.spec
          echo 'source.include_exts = py' >> buildozer.spec
          echo 'version = 1.0.0' >> buildozer.spec
          echo 'requirements = python3,aiohttp,pycryptodome,websockets' >> buildozer.spec
          echo 'orientation = portrait' >> buildozer.spec
          echo 'fullscreen = 0' >> buildozer.spec
          echo 'android.permissions = INTERNET, FOREGROUND_SERVICE' >> buildozer.spec
          echo 'android.api = 31' >> buildozer.spec
          echo 'android.minapi = 26' >> buildozer.spec
          echo 'android.ndk = 23b' >> buildozer.spec
          echo '' >> buildozer.spec
          echo '[buildozer]' >> buildozer.spec
          echo 'log_level = 2' >> buildozer.spec
          echo 'warn_on_root = 1' >> buildozer.spec
          echo 'build_dir = ./.buildozer' >> buildozer.spec
          echo 'bin_dir = ./bin' >> buildozer.spec
          echo 'android.accept_sdk_license = True' >> buildozer.spec
          echo 'android.archs = arm64-v8a, armeabi-v7a' >> buildozer.spec
          echo 'android.sdk_path = /opt/android-sdk' >> buildozer.spec

      - name: Build APK
        run: |
          export ANDROID_SDK_ROOT=/opt/android-sdk
          export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: tg-ws-proxy-android
          path: bin/*.apk
